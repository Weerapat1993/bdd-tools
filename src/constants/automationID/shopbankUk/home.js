import { name } from '../../../../package.json'

const home = {
  sceneHome: `${name}.shopbankUk.home.sceneHome`,
  hamburgerMenu: `${name}.shopbankUk.home.hamburgerMenu`,
  shopTab: `${name}.shopbankUk.home.shopTab`,
}

export default home
