import { defineSupportCode } from 'cucumber'
import { expect } from 'chai'
import { clickLoginWithEmailButton } from './functions'

defineSupportCode(({ Given, When, Then }) => {

  Given(/^Given a member (who is signed in) is on "Personal Menu" and their favourites is NOT empty$/, async () => {
    // await
  })

  Given(/^Given a member tap "My Favourite" menu$/, async () => {
    // await
  })

  When(/^When 1. System navigate to My Favourite page$/, async () => {
    // await
  })

})
